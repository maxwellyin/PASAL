# Legacy Layout

This directory documents the older project organization that revolved around the `v4/` experiment folder.

The original `v4/` layout is kept only as historical reference for the paper-era workflow. The current public-facing structure is:

- [`scripts/`](../scripts/)
- [`pasal/`](../pasal/)

If you are browsing the repository for understanding or reuse, start with [`scripts/`](../scripts/) and [`pasal/`](../pasal/) instead of the archived experiment tree.
