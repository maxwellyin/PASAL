# `v4` Legacy Directory

This directory preserves the older paper-era experiment layout.

For the current public-facing repository structure, use:

- [`scripts/`](../../scripts/)
- [`pasal/`](../../pasal/)

The `v4/` tree is kept only as historical reference and should not be treated as the main project interface.
